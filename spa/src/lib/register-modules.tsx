/* eslint-disable react-refresh/only-export-components */
// Transitional shim. Real implementation lives in `./register-modules/`
// (split per Task 1.0b — register-modules.tsx exceeded 460 lines).
// Existing callers can keep importing from `lib/register-modules` while we
// migrate import paths in follow-up tasks.
export * from './register-modules/index'
