# D3 Peer Pairing — Mutation-Test Record

- Branch: `worktree-peer-pairing-d3`
- HEAD sha: `fc3155ee013fbffb027fb490e683919f05e254e2` (M1–M30); M9/M10/M11/M12/M22/M28/M31
  re-run against the final-review C1/I1 fix (derived `last_inbound_auth`) — the code of the
  commit `fix(peers): the rotation record is bound to the token it saw …` that also carries this
  record; those rows are marked **(re-run)**
- Date: 2026-09-18

Task 7 proves the tests written in Tasks 1–6 guard what they claim (spec
§8.3: mutation tests are a deliverable). Each row below was applied as a
temporary one-edit mutation with the Edit tool, run against the named
test(s) with `go test -count=1` (no `-race`, no cache), confirmed FAIL (or
recorded as an intended exception), then reverted with `git checkout --
<file>` before the next row. After all thirty rows, `git status --short`
was empty (Step 2).

Three mutations (M4, M5/M7, M18) could not be applied as literally
one-line-deleted-only edits without breaking compilation (an unused local
variable/import). In each case the closest one-edit equivalent was applied
instead — discarding the now-unused value via `_` — and is noted in the
table; the assertion outcome is identical to what the brief anticipated.

| # | Mutation (exact edit) | File | Command | Result | Failing assertion (as printed) |
|---|---|---|---|---|---|
| M1 | Deleted the `InboundTokenPrev` comparison block inside `MatchInboundToken`'s loop | `internal/config/config.go` | `go test -count=1 ./internal/config/... -run '^(TestMatchInboundToken_PendingRotation_BothTokensSameAlias\|TestMatchInboundToken_LastMatchWinsAcrossPrev\|TestMatchInboundToken_PrevOnlyStillAuthenticates)$'` | FAIL (all three) | `peers_test.go:354: prev: ok=false alias="" usedPrev=false; want ok air true` (`PendingRotation_BothTokensSameAlias`); `peers_test.go:372: got ok=true alias="one" usedPrev=false; want ok three true` (`LastMatchWinsAcrossPrev`); `peers_test.go:384: got ok=false alias="" usedPrev=false; want ok air true` (`PrevOnlyStillAuthenticates`) |
| M2 | Changed `matchIdx, usedPrev = i, false` to `return p.Hosts[i], usedPrev, true` on the first current-token match (early exit) in `MatchInboundToken` | `internal/config/config.go` | `go test -count=1 ./internal/config/... -run '^TestMatchInboundToken_LastMatchWinsAcrossPrev$'` | FAIL | `peers_test.go:372: got ok=true alias="one" usedPrev=false; want ok three true` |
| M3 | Dropped the `out.Peers.Hosts[i].InboundTokenPrev = ""` line in `Redacted` | `internal/config/config.go` | `go test -count=1 ./internal/config/... -run '^TestRedacted_BlanksInboundTokenPrev$'` | FAIL | `peers_test.go:397: Redacted left a token value: {Alias:air URL: HostID: Token: InboundToken: InboundTokenPrev:in-prev AllowBypass:false}` |
| M4 | Changed `UsedPrevToken: usedPrev` to `UsedPrevToken: false` in `PeerAuth`'s host-principal branch. **Deviation**: leaving `UsedPrevToken: false` as written made `usedPrev` a declared-and-unused local, so the destructuring was changed to `host, _, matched := peers().MatchInboundToken(bearer)` (closest one-edit equivalent; net effect identical — the field is always `false`) | `internal/middleware/peer_auth.go` | `go test -count=1 ./internal/middleware/... -run '^TestPeerAuthPrevTokenSetsUsedPrevToken$'` | FAIL | `peer_auth_test.go:285: tok-old: principal = {Kind:host Alias:beta HostID:host-beta UsedPrevToken:false}, want host beta UsedPrevToken=true` |
| M5 | Deleted the `if !req.Force && last != inboundAuthCurrent { …409… }` gate in `handleRotateCommit`. **Deviation**: this left `req` unused in that function, so `_ = req` was added right after `decodeGateRequest` (closest one-edit equivalent; the deleted gate is the only change to behavior) | `internal/module/peers/hosts_rotate.go` | `go test -count=1 ./internal/module/peers/... -run '^(TestRotateCommit_Gate\|TestPeerAuth_EndToEnd_PendingRotation_CommitGate)$'` | FAIL (both) | `hosts_rotate_test.go:244: commit with no dial = 200 ""; want 409 rotation unconfirmed` (`TestRotateCommit_Gate`); `hosts_rotate_test.go:432: commit after old-token dial = 200; want 409` (`TestPeerAuth_EndToEnd_PendingRotation_CommitGate`) |
| M6 | Deleted `h.InboundTokenPrev = ""` inside `handleRotateCommit`'s success path (commit that does not clear prev) | `internal/module/peers/hosts_rotate.go` | `go test -count=1 ./internal/module/peers/... -run '^TestRotateCommit_Gate$'` | FAIL | `hosts_rotate_test.go:263: after commit: cur "pdxp_11111111111111111111111111111111" prev "pdxp_00000000000000000000000000000000"; want cur=new prev=""` |
| M7 | Deleted the `!= inboundAuthPrev` gate in `handleRotateCancel`. **Deviation**: same as M5 — `_ = req` added to keep `req` used after the gate that read `req.Force` was removed | `internal/module/peers/hosts_rotate.go` | `go test -count=1 ./internal/module/peers/... -run '^TestRotateCancel_Gate$'` | FAIL | `hosts_rotate_test.go:292: cancel with no dial = 200 ""; want 409 rotation unconfirmed` |
| M8 | Dropped `h.InboundToken = h.InboundTokenPrev` in `handleRotateCancel` (cancel that does not restore) | `internal/module/peers/hosts_rotate.go` | `go test -count=1 ./internal/module/peers/... -run '^TestRotateCancel_Gate$'` | FAIL | `hosts_rotate_test.go:306: after cancel: cur "pdxp_11111111111111111111111111111111" prev ""; want cur=old prev=""` |
| M9 **(re-run)** | In `handleRotateCancel`, treat `""` as prev: gate became `if last := m.lastInboundAuthLocked(*h); !req.Force && last != inboundAuthPrev && last != ""` — the row the memory file/brief says must stay red | `internal/module/peers/hosts_rotate.go` | `go test -count=1 ./internal/module/peers/... -run '^TestRotateCancel_Gate$'` | FAIL (as required) | `hosts_rotate_test.go:301: cancel with no dial = 200 ""; want 409 rotation unconfirmed` |
| M10 **(re-run)** | In `handleRotateCommit`, treat `""` as current: gate became `if last := m.lastInboundAuthLocked(*h); !req.Force && last != inboundAuthCurrent && last != ""` | `internal/module/peers/hosts_rotate.go` | `go test -count=1 ./internal/module/peers/... -run '^TestRotateCommit_Gate$'` | FAIL | `hosts_rotate_test.go:253: commit with no dial = 200 ""; want 409 rotation unconfirmed` |
| M11 **(re-run)** | Deleted `m.resetInboundAuth(h.Alias)` in `handleRotateHost`. **First re-run went GREEN**: the test's pre-rotate dial used `curPrincipal` (fingerprint of `rotTokCur`), a token that entry never had, so under the derived record the note read `""` with or without the reset. The fixture was wrong for the new design, not the code: the dial now uses `principalFor("air", rotTokPrev)` (the entry's real token, "current" then, "prev" after the rotate) and the row went RED as intended | `internal/module/peers/hosts_rotate.go` | `go test -count=1 ./internal/module/peers/... -run '^TestRotate_MintsFreshToken_PrevIsOld_RecordReset$'` | FAIL (after fixture fix) | `hosts_rotate_test.go:224: after rotate row = {...RotationPending:true LastInboundAuth:prev}; want pending, record reset to ""` |
| M12 **(re-run)** | In `noteInboundAuth`, made the record sticky (`if _, seen := m.lastInbound[p.Alias]; seen { m.rotMu.Unlock(); return }` before the write) | `internal/module/peers/rotation.go` | `go test -count=1 ./internal/module/peers/... -run '^(TestHandlePeers_HostPrincipalRecordsLastInboundAuth\|TestRotateCommit_Gate)$'` | FAIL (both) | `hosts_rotate_test.go:98: after prev dial: last_inbound_auth = "current", want prev (most recent wins)`; `hosts_rotate_test.go:276: commit after NEW-token dial = 409; body={"error":"rotation unconfirmed"}` |
| M13 | Moved `m.noteInboundAuth(principal)` in `handleDeliver` from immediately after `PrincipalFrom` to below the principal `switch` (after every principal-shape refusal) | `internal/module/peers/deliver.go` | `go test -count=1 ./internal/module/peers/... -run '^TestHandleDeliver_RefusedDeliveryStillRecords$'` | FAIL | `hosts_rotate_test.go:113: refused delivery did not record: last_inbound_auth = "", want prev` |
| M14 | In `hostRowLocked` (the function `hostRowFor` delegates to), changed `row.RotationPending = h.InboundTokenPrev != ""` to `row.RotationPending = false` always | `internal/module/peers/rotation.go` | `go test -count=1 ./internal/module/peers/... -run '^TestHostRow_RotationPendingAndLastInboundAuth_NoTokenValues$'` | FAIL | `hosts_rotate_test.go:65: row = {...RotationPending:false LastInboundAuth:}; want rotation_pending=true last_inbound_auth=""` |
| M15 | In `handleRotateHost`, skipped the `if h.InboundTokenPrev != "" { …409… }` check | `internal/module/peers/hosts_rotate.go` | `go test -count=1 ./internal/module/peers/... -run '^TestRotate_409WhenPending_404Unknown_403Host$'` | FAIL | `hosts_rotate_test.go:227: rotate while pending = 200 ""` |
| M16 | In `handlePutHost`'s closure, dropped `\|\| h.InboundToken != existingInboundToken` from the concurrent-change re-check (kept `h.URL != existingURL` alone). **Deviation**: left `existingInboundToken` unused, so `_ = existingInboundToken` was added (closest one-edit equivalent) | `internal/module/peers/hosts.go` | `go test -count=1 ./internal/module/peers/... -run '^TestRotate_DuringBlockedPut_Put409_TokensUntouched$'` | FAIL | `hosts_rotate_test.go:382: PUT = 200 ""; want 409 entry changed concurrently` |
| M17 | In `fetchHostResult`, changed `bound := func(s string) string { return boundRemoteText(redactSecret(s, h.Token)) }` to `bound := func(s string) string { return boundRemoteText(s) }` (no redact) | `internal/module/peers/module.go` | `go test -count=1 ./internal/module/peers/... -run '^(TestHandleVerifyHost_PeerEchoesOurTokenIsRedacted\|TestHandlePeers_ScopeAll_PeerEchoesOurTokenIsRedacted)$'` | FAIL (both) | `hosts_verify_test.go:297: status=200 body={...,"error":"peer: echo pdxp_deadbeef...","self_alias":"pdxp_deadbeef..."}` (verify); `module_test.go:1121: outbound token echoed into the aggregate: {...self_alias":"self-pdxp_deadbeef...", ...}` (scope=all) |
| M18 | In `redactSecret`, made it return `s` unchanged regardless of `secret`. **Deviation**: this left the `strings` import unused, so `_ = strings.ReplaceAll` was added inside the function body (closest one-edit equivalent) | `internal/module/peers/module.go` | `go test -count=1 ./internal/module/peers/... -run '^(TestHandleVerifyHost_PeerEchoesOurTokenIsRedacted\|TestHandlePeers_ScopeAll_PeerEchoesOurTokenIsRedacted)$'` | FAIL (both) | `hosts_verify_test.go:297: status=200 body={...,"error":"peer: echo pdxp_deadbeef...", ...}`; `module_test.go:1121: outbound token echoed into the aggregate: {...}` |
| M19 | In `parsePeersInvocation`, dropped the `if inv.rotateCommit && inv.rotateCancel { return ..., false }` rejection | `cmd/pdx/peers.go` | `go test -count=1 ./cmd/pdx/... -run '^TestParsePeersInvocation_Rotate$'` | FAIL | `peers_test.go:2050: [host rotate air --commit --cancel]: ok=true want false` |
| M20 | In `runPeersHostRotate`, changed `json.Marshal(cliRotateGateRequest{Force: inv.rotateForce})` to always send `Force: true` | `cmd/pdx/peers.go` | `go test -count=1 ./cmd/pdx/... -run '^TestRunPeersHostRotate_Commit_UnconfirmedExplainsOnPeer$'` | FAIL | `peers_test.go:2096: commit sent force without --force: {"force":true}` |
| M21 | In `rotationCell`, changed the `LastInboundAuth == "current"` case to also return `"pending"` (instead of `"pending, confirmed"`) | `cmd/pdx/peers.go` | `go test -count=1 ./cmd/pdx/... -run '^TestFormatHostsTable_RotationColumn$'` | FAIL | table renders `pending` for both the unconfirmed and confirmed rows (`b` and `c`), where the test wants `c` to read `pending, confirmed` |
| M22 **(re-run, replaced)** | In `lastInboundAuthLocked`, dropped the `prev` branch (compare `fp` only against `InboundToken`). Replaces the original M22 (`confirmCancelledRotation` no-op) — that function was deleted by the C1/I1 fix; the derived record has no rewrite to disable | `internal/module/peers/rotation.go` | `go test -count=1 ./internal/module/peers/... -run '^(TestRotateCancel_Gate\|TestRotate_DialAuthenticatedBeforeRotateNotesAfterReset_ReadsPrev)$'` | FAIL (both) | `hosts_rotate_test.go:319: cancel after OLD-token dial = 409; body={"error":"rotation unconfirmed"}` (`TestRotateCancel_Gate`); `hosts_rotate_test.go:606: note for tX landing after the rotate reads "", want "prev" (tX is now the entry's prev)` (C1 test) |
| M23 | Dropped `m.renameInboundAuth(alias, req.Alias)` in `handlePutHost`'s rename branch | `internal/module/peers/hosts.go` | `go test -count=1 ./internal/module/peers/... -run '^TestInboundAuthRecord_FollowsRenameAndDelete$'` | FAIL | `hosts_rotate_test.go:144: record did not follow the rename: ""` |
| M24 | Dropped `m.resetInboundAuth(alias)` in `handleDeleteHost` | `internal/module/peers/hosts.go` | `go test -count=1 ./internal/module/peers/... -run '^TestInboundAuthRecord_FollowsRenameAndDelete$'` | FAIL | `hosts_rotate_test.go:153: re-created entry inherited a record: "current"` |
| M25 | Deleted the `for i := range peers { redactRecord(&peers[i], h.Token) }` loop in `fetchHostResult` | `internal/module/peers/module.go` | `go test -count=1 ./internal/module/peers/... -run '^TestHandlePeers_ScopeAll_PeerEchoesOurTokenIsRedacted$'` | FAIL | `module_test.go:1121: outbound token echoed into the aggregate: {...,"session_name":"sess-pdxp_deadbeef...", ...}` (row fields no longer redacted) |
| M26 | In the fetch-error branch, changed `Error: bound(err.Error())` to `Error: redactSecret(err.Error(), h.Token)` (redact only, no length bound) | `internal/module/peers/module.go` | `go test -count=1 ./internal/module/peers/... -run '^TestHandleVerifyHost_ErrorBranchesRedactAndBound$'` | FAIL | `hosts_verify_test.go:285: transport: status=200 len=5108 leaked=false` (body far exceeds `2*maxRemoteTextBytes`; token itself still not leaked, confirming the test guards the bound, not just the redaction) |
| M27 | Moved `m.noteInboundAuth(principal)` in `handleDeliver` from immediately after `PrincipalFrom` to below the `stopCtx.Err()` check | `internal/module/peers/deliver.go` | `go test -count=1 ./internal/module/peers/... -run '^TestHandleDeliver_StoppingStillRecords$'` | FAIL | `hosts_rotate_test.go:128: stopping refusal did not record: ""` |
| M28 **(re-run, replaced)** | In `handleRotateCancel`'s closure, added `delete(m.lastInbound, h.Alias)` right after the swap (`h.InboundTokenPrev = ""`) — a cancel that "starts a new epoch" the way rotate does, before the write. Replaces the original M28 (moving `confirmCancelledRotation` inside the closure): there is no rewrite step any more, so the original failure mode (a record describing an unpersisted swap) is impossible by construction; this is the nearest live mistake in the same spot | `internal/module/peers/hosts_rotate.go` | `go test -count=1 ./internal/module/peers/... -run '^(TestRotateCancel_FailedWriteLeavesRecordAndGateIntact\|TestRotateCancel_Gate)$'` | FAIL (both) | `hosts_rotate_test.go:547: record no longer says the peer is on the OLD token despite failed write: last_inbound_auth = "", want "prev"`; `hosts_rotate_test.go:331: after cancel row = {...RotationPending:false LastInboundAuth:}` (want `"current"`) |
| M29 | In `fetchHostResult`'s final return, changed `HostID: bound(resultHostID)` to `HostID: resultHostID` (drop bound/redact) | `internal/module/peers/module.go` | `go test -count=1 ./internal/module/peers/... -run '^(TestHandlePeers_ScopeAll_LearnedHostIDRedacted\|TestHandleVerifyHost_LearnedHostIDRedacted)$'` | FAIL (both) | `hosts_verify_test.go:316: status=200 body={...,"host_id":"pdxp_deadbeefdeadbeefdeadbeefdeadbeef", ...}` (verify route); `module_test.go:1149: outbound token echoed into the aggregate host_id: {...,"host_id":"pdxp_deadbeefdeadbeefdeadbeefdeadbeef", ...}` (scope=all) |
| M30 | In `verifyHost`, deleted the `if redactSecret(env.HostID, token) != env.HostID { return env, "peer returned an invalid host_id" }` refusal | `internal/module/peers/hosts.go` | `go test -count=1 ./internal/module/peers/... -run '^(TestHandleAddHost_LearnedHostIDCarriesOurToken_502NothingPersisted\|TestHandlePutHost_LearnedHostIDCarriesOurToken_502OldValuesKept)$'` | FAIL (both) | `hosts_test.go:535: status = 201, want 502; body={...,"host_id":"pdxp_deadbeef...", ...}` (add); `hosts_test.go:1160: status = 200, want 502; body={...,"host_id":"pdxp_deadbeef...", ...}` (put) |
| M31 **(new)** | In `PeerAuth`'s host-principal branch, changed `TokenFingerprint: config.TokenFingerprint(bearer)` to `TokenFingerprint: ""`. Host principals in the peers module's handler tests are injected with their fingerprint already set, so only the middleware test and the two tests that run the REAL `PeerAuth` chain go red | `internal/middleware/peer_auth.go` | `go test -count=1 ./internal/middleware/... -run '^TestPeerAuthPrevTokenSetsUsedPrevToken$'`; `go test -count=1 ./internal/module/peers/... -run '^(TestPeerAuth_EndToEnd_PendingRotation_CommitGate\|TestRotate_DialAuthenticatedBeforeRotateNotesAfterReset_ReadsPrev)$'` | FAIL (all three) | `peer_auth_test.go:291: tok-new: TokenFingerprint = "", want "2cb3e362808c82e7"`; `hosts_rotate_test.go:447: old-token dial through real PeerAuth = "", want "prev"`; `hosts_rotate_test.go:606: note for tX landing after the rotate reads "", want "prev" (tX is now the entry's prev)` |
| M32a **(codex F2)** | In `HostMatcher` (the matcher `cmd/pdx/http_chain.go` hands to `PeerAuth`), moved the `c.HostAuthObserver(...)` call AFTER `c.CfgMu.RUnlock()` (match under the read-lock, observation outside it) | `internal/module/peers/policy.go` | `go test -race -count=1 ./cmd/pdx/ -run '^TestNewOuterHandler_PeerChainObservesHostMatchUnderCfgRLock$'`; `go test -race -count=1 ./internal/module/peers/ -run '^(TestRotateCommit_RequestMatchedOnOldTokenBeforeHandler_Is409\|TestRotateCancel_RequestMatchedOnNewTokenBeforeHandler_Is409)$'` | FAIL (cmd/pdx); **PASS (peers ×2)** | `http_chain_test.go:591: observer ran with CfgMu free for writing: the match and its observation are not one critical section`. The two peers-package race tests stay GREEN under this variant by construction: their pause sits between `PeerAuth` and the handler, i.e. after the whole matcher, so an observer called right after `RUnlock` has already noted by the time the gate runs. The lock-atomicity itself is only observable from inside the observer (`CfgMu.TryLock()` must fail there), which is what the cmd/pdx test probes. |
| M32b **(codex F2)** | In `HostMatcher`, deleted the `if ok && c.HostAuthObserver != nil { c.HostAuthObserver(...) }` block (observation dropped; only the handlers' second note remains) | `internal/module/peers/policy.go` | same two commands as M32a | FAIL (all three) | `hosts_rotate_test.go:665: commit while an old-token request has authenticated but not reached its handler = 200 ""; want 409 rotation unconfirmed`; `hosts_rotate_test.go:694: cancel while a new-token request has authenticated but not reached its handler = 200 ""; want 409 rotation unconfirmed`; `http_chain_test.go:588: observer calls = []; want exactly one for host-a with the bearer's fingerprint` |
| — | The gates' `rotMu` hold across check→write cannot be turned red by end state (`TestRotateCommit_DialAfterGateCheckIsNotLost` sees the same final row either way); it is enforced by review, and the seam test documents the ordering. Likewise `at` (the `inboundAuth.at` timestamp) is never served. Recorded as **structural, not observable** — not run. | `internal/module/peers/hosts_rotate.go`, `internal/module/peers/rotation.go` | — | not run | — |

## Summary

- 29 of 29 executable rows (M1–M30 minus the structural row) went RED as
  required, including M9/M10 (the two rows the plan explicitly calls out as
  "must stay red" — a naive `!= X && != ""` gate relaxation is caught).
- After the final-review C1/I1 fix (the record stores a token fingerprint
  and `last_inbound_auth` is derived at read time), M9, M10, M11, M12, M22
  and M28 were re-run and M31 added: 7 of 7 RED. M22 and M28 target code
  that no longer exists (`confirmCancelledRotation`) and were replaced by
  the nearest live mistake, noted in their rows. M11's first re-run went
  GREEN because its fixture presented a token the entry never had; the
  fixture was corrected (see the row) and the mutation is RED again.
- 1 row (the `rotMu`-hold / `at`-never-served row) is recorded as
  structural/not-observable per the brief and was not run.
- 3 rows (M4, M5/M7 share one deviation type, M16, M18) required a
  one-line compile-fix (discarding an otherwise-unused value via `_`)
  alongside the named mutation to keep the package building; each is noted
  inline above. None of these deviations changed the mutation's observable
  behavior — they only kept the mutated file compiling so the named test
  could actually run against it.

- After the codex F2 fix (a host-token match and its observation are one
  critical section under `CfgMu.RLock`; `HostMatcher` in
  `internal/module/peers/policy.go`, installed by `cmd/pdx/http_chain.go`;
  observer `noteInboundFP` installed by `Init`), M32a and M32b were run:
  M32b (observation dropped) is RED in the two new peers race tests and in
  the cmd/pdx wiring test; M32a (observation after `RUnlock`) is RED only in
  the cmd/pdx wiring test — see the row for why the race tests cannot see
  it. Both reverted.

`git status --short` after the last row (before adding this file) printed
nothing.
